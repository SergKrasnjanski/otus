apiVersion: v1
kind: Service
metadata:
  name: {{ include "jwt-api.fullname" . }}
  labels:
    {{- include "jwt-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "jwt-api.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.service.port }}
      targetPort: http
